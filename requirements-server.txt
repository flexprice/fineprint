# FinePrint service (Cloud Run). Harness core + web API + GCS state.
# datalab-python-sdk is required by the playground's "upload your own contract" path, which
# OCRs the uploaded PDF live (pipeline.extractor). The benchmark path still uses pre-OCR'd
# pickles; the OCR import stays lazy so only the upload route pulls it in at request time.
datalab-python-sdk>=0.5
openai>=1.40
python-dotenv>=1.0
openpyxl>=3.1
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
google-cloud-storage>=2.16
pymupdf
python-multipart
