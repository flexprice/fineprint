# Keep the source upload small — the free plan caps file uploads at 5000 / 24h.
node_modules
.next
.vercel
*.tsbuildinfo
.env*
# …but keep the empty .env.local the prebuilt deploy lstat's (real env is baked into .vercel/output)
!.env.local

# (CI trigger test — path filter matches fineprint/web/**)
